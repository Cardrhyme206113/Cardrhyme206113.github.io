name: Run Simbers library timing patch

on:
  push:
    paths:
      - .github/workflows/run-simbers-bridge-patch.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - run: python tools/patch_simbers_library_timing.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add simbers-flow-library.html
          git diff --cached --quiet || git commit -m "Show exact simultaneous flow timestamps"
          git push
